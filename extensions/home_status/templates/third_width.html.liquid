{% comment %} Third width: 267x480. A narrow column of switches. {% endcomment %}

<div class="screen dense">
  <div class="title-bar" style="padding: 14px 14px 8px">
    <div class="title" style="font-size: 18px">{{ extension.values.heading | default: "Home" }}</div>
  </div>

  <div class="content content--tight">
    <div class="list">
      {% for thing in home.entities limit: 6 %}
        <div class="list-item" style="padding: 8px 0">
          <span class="with-icon" style="min-width: 0; gap: 6px">
            <span class="i i-{{ thing.icon }} i-xs"></span>
            <span class="truncate t-sm">{{ thing.label }}</span>
          </span>
          <span class="{% if thing.on %}badge{% else %}chip{% endif %}" style="font-size: 10px; padding: 1px 5px">
            {% if thing.on %}ON{% else %}OFF{% endif %}
          </span>
        </div>
      {% endfor %}
    </div>
  </div>

  {% comment %}
    Where another extension gets to say something on this screen. Glyphs like
    dashboard warning lights, with the loudest one's words beside them when the
    shape is wide enough to carry them.
  {% endcomment %}
  {% if notices.size > 0 %}
    <div class="notice-strip">
      {% for notice in notices %}
        {% if notice.overflow %}
          <span class="notice notice--more">+{{ notice.overflow }}</span>
        {% else %}
          <span class="notice notice--{{ notice.level }}" title="{{ notice.text }}">
            <span class="i i-{{ notice.icon }}"></span>
          </span>
        {% endif %}
      {% endfor %}

      {% if shape.wide and notices[0].text != blank %}
        <span class="notice-said">{{ notices[0].text }}</span>
      {% endif %}
    </div>
  {% endif %}
</div>
