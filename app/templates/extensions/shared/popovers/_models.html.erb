<%= scope(:popover_default_content, name: :models, label: "Models").render do %>
  <p>The model(s) to render a screen for. This allows you to dynamically create/update a screen for each model you select.</p>
<% end %>
