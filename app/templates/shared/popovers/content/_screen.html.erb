<dialog id="<%= element_id %>"
        class="bit-popover-content bit-popover-screen"
        popover="auto">
  <figure>
    <%= tag.img src: uri, alt: label, width: width, height: height, loading: :lazy %>
    <figcaption><%= label %></figcaption>
  </figure>
</dialog>
