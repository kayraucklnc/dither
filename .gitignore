# Secrets. `.env` holds the database password and must never be committed.
*.env*
!*.env*.tt

.DS_Store
*.log

# Node and Next
node_modules
.next
next-env.d.ts

# Rendered screens, which are a cache keyed by their own content
.storage
